import { CommonEventBuilder } from './common';
import { TextInputEventBuilder } from './test-input';

export const EventBuilder = {
  Common: CommonEventBuilder,
  TextInput: TextInputEventBuilder,
};
